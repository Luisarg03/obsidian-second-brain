{
  "name": "obsidian-second-brain-plugin",
  "version": "0.2.0",
  "description": "OpenCode Plugin API v2 plugin for the ObsidianSecondBraind project: opt-in reads, auto writes.",
  "main": "register.ts",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
